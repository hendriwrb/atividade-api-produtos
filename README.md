# Cria API com produtos
### Inicia API no backend e mostra um produto com nome, categoria e preço no frontend
js, html e css com express e cors

backend/
frontend/

Para executar, entre na pasta backend digitando "cd backend" no terminal, depois digite "node server.js" para iniciar a API.
